import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mangattu.myApp',
  appName: 'Mangattu App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
